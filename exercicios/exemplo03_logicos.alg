programa {
  funcao inicio()
  {
        logico v1 = falso
        logico v2 = falso
        logico v3 = falso

        logico resultado = falso

        resultado = v1 ou v2 ou v3

        escreva (nao resultado)
  }
}
